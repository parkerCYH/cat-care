# Base URL of the parker-api backend (separate repo). Used by both the runtime API
# client (src/api/mutator.ts, src/stores/authStore.ts) and orval.config.ts's codegen
# target defaults to it directly, so this only needs overriding if your backend runs
# somewhere other than localhost:3001.
VITE_API_BASE_URL=http://localhost:3001
