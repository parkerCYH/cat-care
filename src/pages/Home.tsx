// Placeholder only — phase 0 scope. Real home/quick-record UI lands in issue #6.
export function Home() {
  return (
    <div className="px-4 py-6">
      <p className="text-sm text-gray-500">首頁(待建置 — 見 issue #6)</p>
    </div>
  );
}
